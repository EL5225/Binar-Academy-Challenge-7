<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Password</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 10px;
        width: 100%;
        height: 100vh;
      }

      .register {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 8px;
      }

      .button-register {
        margin-top: 10px;
        padding: 3px;
      }

      input {
        padding: 4px;
      }

      .warn {
        color: red;
        font-size: 15px;
      }
    </style>
  </head>
  <body>
    <section class="container">
      <h1>Reset Password</h1>
      <form class="register" action="/api/auth/forgot-password" method="POST">
        <label for="email">Email</label>
        <input type="email" name="email" id="email" />

        <button class="button-register">Kirim</button>

        <span class="warn">*Silahkan cek email setelah mengirim</span>
      </form>
    </section>
  </body>

  <script></script>
</html>
